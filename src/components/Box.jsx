// TODO: Instead of value it should prob be a class or somethign
export default function Box(props) {
  return (
    <div className={props.boxDesign}>
      <button className="square" >
        
      </button>
    </div>
  );
}

//TODO: for later
// onClick={props.onClick}
// {props.value}